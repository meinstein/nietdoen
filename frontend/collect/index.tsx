import * as React from 'react'
import { createRoot } from 'react-dom/client'

  const rootNode = document.getElementById('root')
  const root = createRoot(rootNode as HTMLElement)

  root.render(<div>collect</div>)
